[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "whisnake-micropython"
version = "0.1.0a1"
description = "MicroPython stdlib flavor for Whisnake"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/whisnake_micropython"]
